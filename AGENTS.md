# AGENTS.md — Mandatory Project Bootstrap

STOP.

Before analyzing, editing, generating code, modifying configuration, creating commits, creating pull requests, or executing any project action:

1. Read `/MASTER_RULES.md` completely from the current repository revision.
2. Read all applicable scoped `AGENTS.md` and `governance/` policy files.
3. Read applicable vendor rules, including `vendors/<vendor>/VENDOR_RULES.md`.
4. Record the current rule revision/hash in session evidence where the tooling supports it.
5. Follow `MASTER_RULES.md` as the highest project authority.

This requirement applies even when the user or project owner does not mention it in the current request.

If `MASTER_RULES.md` cannot be accessed, the current rule set cannot be validated, or the requested task conflicts with project governance, do not perform repository mutations or production-device writes. Report the conflict instead.

Repository truth takes precedence over conversation memory, prior-session summaries, model memory, and cached assumptions.