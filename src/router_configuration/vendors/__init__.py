"""Vendor-specific router domains.

Each vendor owns its knowledge, intent semantics, tool catalog, validation and
post-deployment lifecycle. Shared project code must not flatten vendor behavior
into one universal configuration model.
"""
