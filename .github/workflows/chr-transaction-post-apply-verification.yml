name: CHR Transaction Post-Apply Verification

on:
  workflow_dispatch:
  push:
    paths:
      - "src/router_configuration/transaction_*.py"
      - "lab/chr/verify_transaction_post_apply.py"
      - "lab/chr/verify_transaction_runtime_admission.py"
      - "lab/chr/verify_mutation_rollback.py"
      - "lab/chr/verify_render_dry_run.py"
      - "lab/chr/verify_render_dry_run_chunked.py"
      - "lab/chr/build_renderer_syntax_fixture.py"
      - "tests/test_chr_transaction_post_apply_contract.py"
      - ".github/workflows/chr-transaction-post-apply-verification.yml"

permissions:
  contents: read

jobs:
  chr-transaction-post-apply-verification:
    concurrency:
      group: router-config-chr-transaction-post-apply-${{ github.ref }}
      cancel-in-progress: false
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CHR_VERSION: "7.24.1"
      CHR_ARCHIVE: /tmp/chr-7.24.1.img.zip
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install project and QEMU
        run: |
          set -euo pipefail
          python -m pip install -e .
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends qemu-system-x86 qemu-utils unzip
      - name: Restore official MikroTik CHR archive cache
        id: chr-cache
        uses: actions/cache/restore@v4
        with:
          path: /tmp/chr-${{ env.CHR_VERSION }}.img.zip
          key: chr-${{ runner.os }}-${{ env.CHR_VERSION }}
      - name: Download official MikroTik CHR image on cache miss
        if: steps.chr-cache.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          curl -4 --http1.1 -fL \
            --connect-timeout 15 \
            --max-time 180 \
            --retry 1 \
            --retry-delay 2 \
            --retry-all-errors \
            "https://download.mikrotik.com/routeros/${CHR_VERSION}/chr-${CHR_VERSION}.img.zip" \
            -o "$CHR_ARCHIVE"
      - name: Prepare official CHR image
        run: |
          set -euo pipefail
          test -s "$CHR_ARCHIVE"
          sha256sum "$CHR_ARCHIVE" > /tmp/chr-download.sha256
          unzip -p "$CHR_ARCHIVE" > /tmp/chr.img
          test -s /tmp/chr.img
      - name: Persist verified CHR archive cache
        if: success() && steps.chr-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: /tmp/chr-${{ env.CHR_VERSION }}.img.zip
          key: chr-${{ runner.os }}-${{ env.CHR_VERSION }}
      - name: Boot isolated CHR in QEMU snapshot mode
        run: |
          set -euo pipefail
          qemu-system-x86_64 \
            -accel tcg,thread=multi \
            -smp 1 \
            -m 256 \
            -snapshot \
            -drive file=/tmp/chr.img,format=raw,if=virtio \
            -netdev user,id=net0,hostfwd=tcp:127.0.0.1:9580-:80 \
            -device virtio-net-pci,netdev=net0 \
            -netdev user,id=net1 \
            -device virtio-net-pci,netdev=net1 \
            -netdev user,id=net2 \
            -device virtio-net-pci,netdev=net2 \
            -display none \
            -serial file:/tmp/chr-transaction-post-apply-serial.log \
            -daemonize \
            -pidfile /tmp/chr-transaction-post-apply.pid
      - name: Wait for disposable CHR REST
        run: |
          set -euo pipefail
          ready=0
          for attempt in $(seq 1 90); do
            if curl -fsS --max-time 2 --user 'admin:' \
              http://127.0.0.1:9580/rest/system/resource \
              > /tmp/chr-transaction-post-apply-resource.json; then
              ready=1
              break
            fi
            sleep 2
          done
          if [ "$ready" -ne 1 ]; then
            echo 'Disposable CHR REST did not become reachable' >&2
            cat /tmp/chr-transaction-post-apply-serial.log >&2 || true
            exit 1
          fi
          curl -fsS --max-time 2 --user 'admin:' \
            http://127.0.0.1:9580/rest/interface \
            > /tmp/chr-transaction-post-apply-interfaces.json
      - name: Prove independent post-apply verification
        run: |
          set -euo pipefail
          mkdir -p evidence
          python lab/chr/verify_transaction_post_apply.py \
            --admin-url http://127.0.0.1:9580 \
            --workflow-sha "$GITHUB_SHA" \
            --output evidence/chr-transaction-post-apply-verification.json
      - name: Validate sanitized post-apply artifact
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path

          path = Path("evidence/chr-transaction-post-apply-verification.json")
          payload = json.loads(path.read_text(encoding="utf-8"))
          assert payload["ok"] is True
          assert payload["acceptance"] == "PASS"
          assert payload["workflow_sha"] == "${{ github.sha }}"
          assert payload["post_apply_verification_claimed"] is True
          assert payload["management_survival_after_apply_claimed"] is True
          assert payload["management_survival_during_apply_claimed"] is False
          assert payload["routed_data_plane_claimed"] is False
          assert payload["recovery_verification_claimed"] is False
          assert payload["lifecycle"]["phase"] == "verified"
          post = payload["post_apply_verification"]
          assert post["fresh_rest_session"] is True
          assert post["apply_observation_reused_for_verification_state"] is False
          assert post["management_rest_ok"] is True
          assert post["required_interfaces_running"] is True
          assert post["intended_state_ok"] is True
          assert post["post_state_matches_apply_observation"] is True
          assert post["routed_data_plane_claimed"] is False
          assert post["raw_configuration_recorded"] is False
          assert post["secret_values_present"] is False
          assert payload["lab_cleanup"]["completed"] is True
          assert payload["lab_cleanup"]["baseline_digest_restored"] is True
          assert payload["lab_cleanup"]["changes_transaction_lifecycle"] is False
          assert payload["operator_attestation_claimed"] is False
          assert payload["production_writer_available"] is False
          assert payload["transport_exposed_to_product"] is False
          assert payload["physical_router_targeted"] is False
          assert payload["production_allowed"] is False
          assert payload["write_authorized"] is False
          rendered = json.dumps(payload, sort_keys=True).lower()
          for forbidden in ("private_key", "password", "token", ".backup"):
              assert forbidden not in rendered, forbidden
          print(json.dumps({
              "acceptance": payload["acceptance"],
              "workflow_sha": payload["workflow_sha"],
              "transaction_id": payload["transaction"]["transaction_id"],
              "post_state_sha256": post["post_state_sha256"],
              "lifecycle_phase": payload["lifecycle"]["phase"],
              "cleanup_baseline_sha256": payload["lab_cleanup"]["cleanup_state_sha256"],
          }, indent=2, sort_keys=True))
          PY
      - name: Preserve sanitized post-apply evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: chr-transaction-post-apply-${{ github.sha }}
          path: |
            evidence/chr-transaction-post-apply-verification.json
            /tmp/chr-transaction-post-apply-resource.json
            /tmp/chr-transaction-post-apply-interfaces.json
            /tmp/chr-download.sha256
          if-no-files-found: warn
          retention-days: 14
      - name: Stop disposable CHR
        if: always()
        run: |
          if [ -f /tmp/chr-transaction-post-apply.pid ]; then
            kill "$(cat /tmp/chr-transaction-post-apply.pid)" 2>/dev/null || true
          fi
