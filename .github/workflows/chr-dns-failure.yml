name: CHR DNS Failure

on:
  push:
  pull_request:
    paths:
      - "src/router_configuration/lab_probes.py"
      - "lab/chr/dns_probe.py"
      - "lab/chr/udp_tag_server.py"
      - "lab/chr/udp_flow_probe.py"
      - "lab/chr/run_dns_failure_acceptance.sh"
      - "lab/chr/verify_dns_failure_acceptance.py"
      - ".github/workflows/chr-dns-failure.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  chr-dns-failure:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'pull_request' || startsWith(github.event.head_commit.message, 'ci(chr-dns):')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: router-config-chr-dns-failure-${{ github.ref }}
      cancel-in-progress: false
    env:
      CHR_VERSION: "7.24.1"
      CHR_ARCHIVE: /tmp/chr-7.24.1.img.zip
      EVIDENCE_DIR: ${{ github.workspace }}/evidence/chr-dns-failure
      FLOW_COUNT: "30"
      FLOW_TIMEOUT: "0.25"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install project and DNS-failure lab dependencies
        run: |
          set -euo pipefail
          python -m pip install -e .
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends qemu-system-x86 qemu-utils unzip iproute2
          sudo -n true

      - name: Restore official MikroTik CHR archive cache
        id: chr-cache
        uses: actions/cache/restore@v4
        with:
          path: /tmp/chr-${{ env.CHR_VERSION }}.img.zip
          key: chr-${{ runner.os }}-${{ env.CHR_VERSION }}

      - name: Run DNS service-failure acceptance
        run: |
          set -euo pipefail
          bash lab/chr/run_dns_failure_acceptance.sh

      - name: Persist verified CHR archive cache
        if: success() && steps.chr-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: /tmp/chr-${{ env.CHR_VERSION }}.img.zip
          key: chr-${{ runner.os }}-${{ env.CHR_VERSION }}

      - name: Preserve DNS failure evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: chr-dns-failure-${{ github.sha }}
          path: |
            evidence/chr-dns-failure/
            /tmp/chr-packet-flow-serial.log
            /tmp/chr-packet-flow-wan10-server.log
            /tmp/chr-packet-flow-wan1-server.log
            /tmp/chr-dns-failure-wan10.log
            /tmp/chr-dns-failure-wan1.log
          if-no-files-found: warn
          retention-days: 14
