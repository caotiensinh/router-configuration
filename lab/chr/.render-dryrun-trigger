RouterOS CHR 7.24.1 recursive Dual-WAN renderer syntax dry-run validation with chunked file round-trip
Purpose: execute the dedicated CHR Render Dry Run workflow against the 17-command fixture covering interface lists, static WAN addresses, routing tables, recursive health-probe routes and recursive default routes. Temporary script files larger than 4 KiB must be verified through RouterOS /file/read in bounded chunks with exact byte and SHA256 equality.
Acceptance target: generated RouterOS script passes chunk-verified file round-trip and transitions verdict PENDING -> OK; documented invalid fixture `this` transitions PENDING -> ERROR|<detail>; configuration digest across interface/list, interface/list/member, ip/address, routing/table and ip/route is identical before and after; all temporary RouterOS files are verified absent; the only generation blocker is intentionally deferred PCC; no production write authorization.
