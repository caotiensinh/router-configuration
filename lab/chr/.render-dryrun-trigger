RouterOS CHR 7.24.1 generated renderer syntax dry-run async verdict validation
Purpose: execute the dedicated CHR Render Dry Run workflow after treating /rest/execute ret as an asynchronous job handle and polling the temporary RouterOS verdict file with a bounded timeout.
Acceptance target: generated RouterOS topology script transitions verdict PENDING -> OK; documented invalid fixture `this` transitions PENDING -> ERROR|<detail>; interface-list configuration digest is identical before and after; all temporary RouterOS files are verified absent; no production write authorization.
