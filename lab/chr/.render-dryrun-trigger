RouterOS CHR 7.24.1 recursive Dual-WAN renderer syntax dry-run validation
Purpose: execute the dedicated CHR Render Dry Run workflow against the 17-command fixture covering interface lists, static WAN addresses, routing tables, recursive health-probe routes and recursive default routes.
Acceptance target: generated RouterOS script transitions verdict PENDING -> OK; documented invalid fixture `this` transitions PENDING -> ERROR|<detail>; configuration digest across interface/list, interface/list/member, ip/address, routing/table and ip/route is identical before and after; all temporary RouterOS files are verified absent; the only generation blocker is intentionally deferred PCC; no production write authorization.
