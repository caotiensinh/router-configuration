RouterOS CHR 7.24.1 recursive Dual-WAN renderer syntax validation after add-only routing-table correction
Purpose: validate the full 17-command recursive failover fixture on the exact current main after per-command CHR diagnostics proved that only the two routing-table conditional commands failed. The renderer now creates a missing dedicated table with the RouterOS fib switch and never mutates flags on an existing table; readiness blocks reuse of an existing non-FIB table. Temporary script files larger than 4 KiB are verified through RouterOS /file/read in bounded chunks with exact byte and SHA256 equality.
Acceptance target: all 17 generated RouterOS commands pass the full import dry-run; documented invalid fixture `this` still produces the expected error; configuration digest across interface/list, interface/list/member, ip/address, routing/table and ip/route remains identical before and after; all temporary RouterOS files are absent; only capacity-weighted PCC remains intentionally deferred; production write authorization remains false.
