RouterOS CHR 7.24.1 clean read-only admission final retry
Purpose: rerun isolated CHR Clean Admission after the evaluator was hardened to require both declared and RouterOS-observed effective policy exactly {read,api,rest-api}.
Acceptance target: fresh snapshot; dedicated least-privilege identity; HTTPS with CA verification; GET-only discovery; evidence integrity; machine provenance; zero admission-phase mutation.
This file contains no credentials, image data, production configuration, or write authorization.
